export async function initDatabase() {
  // Firestore is schema-less, so no explicit initialization is strictly required.
  // We can use this space to seed initial data if needed.
  return Promise.resolve();
}
